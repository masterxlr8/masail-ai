# Copy to `.env` and fill in. `.env` is gitignored; this template is not.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Absent -> embed.py and search.py fall back to the local
# sentence-transformers model (BAAI/bge-small-en-v1.5, 384 dims, free, CPU).
# OPENAI_API_KEY=sk-...
